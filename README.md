# PalindromeCheckApp
🔁 Palindrome Check App
📌 Project Objective

The objective of the Palindrome Check App is to build a simple and interactive application that checks whether a given word, phrase, or number is a palindrome.

A palindrome is a string that reads the same forward and backward (ignoring spaces, punctuation, and case sensitivity).

Examples:

madam ✅

racecar ✅

hello ❌

A man a plan a canal Panama ✅

🎯 Project Flow
1️⃣ User Input

The user enters a word, sentence, or number into the input field.

2️⃣ Data Preprocessing

Remove spaces.

Remove special characters (if required).

Convert all letters to lowercase.

Example:

Input: "A man a plan a canal Panama"
Processed: "amanaplanacanalpanama"
3️⃣ Reverse the String

Reverse the processed string using:

String slicing

Loop

Built-in reverse method (depending on implementation language)

4️⃣ Comparison

Compare original processed string with reversed string.

5️⃣ Display Result

If both are equal → Display: ✅ "It is a Palindrome"

If not equal → Display: ❌ "Not a Palindrome"

⚙️ Procedure / Steps Done

Designed user interface (Input field + Button + Result display).

Added event listener to button click.

Captured user input.

Cleaned and formatted the input string.

Implemented palindrome checking logic.

Displayed result dynamically on the screen.

Tested with multiple test cases.

💻 Technologies Used

HTML (Structure)

CSS (Styling)

JavaScript (Logic)

UC1: Application Entry & Welcome Message
Goal: Display a welcome message and app details at startup. 
